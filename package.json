{
  "name": "wotan-insights-phase1",
  "private": true,
  "scripts": {
    "dev:frontend": "cd apps/frontend && npm run dev",
    "dev:backend": "cd apps/backend && npm run start:dev",
    "install:all": "npm install && cd apps/frontend && npm install && cd ../backend && npm install"
  }
}
